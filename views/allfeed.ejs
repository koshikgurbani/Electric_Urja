<%- include('header') %>

<% admins.forEach(tocheck); %>

<% var pos=0,i=0 %>
<% function tocheck(admin){%>
  <% if(admin.login_id == ans){%>
      <%  pos=i; %>
  <% }%>
  <% i++ %>
<% } %>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">
      <img src="/docs/4.0/assets/brand/bootstrap-logo.svg" alt="" width="30" height="24" class="d-inline-block align-top">
      <%=admins[pos].admin_id%>
  </a>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav" >
    <ul class="nav justify-content-center>
      <li class="nav-item">
        <script>
          var val = '<%=admins[pos].login_id%>';
          var loc="/admin"+val;
          document.write('<a class="nav-link" href="' + loc + '">Home</a>');
        </script>
      </li>
      <li class="nav-item">
        <script>
          var val = '<%=admins[pos].login_id%>';
          var loc="/adm"+val;
          document.write('<a class="nav-link" href="' + loc + '">My Profile</a>');
        </script>
      </li>
      <li class="nav-item">
        <script>
          var val = '<%=admins[pos].login_id%>';
          var loc="/all"+val;
          document.write('<a class="nav-link" href="' + loc + '">All Feedbacks</a>');
        </script>
      </li>
      <li class="nav-item">
        <script>
          var val = '<%=admins[pos].login_id%>';
          var loc="/change"+val;
          document.write('<a class="nav-link" href="' + loc + '">All bills</a>');
        </script>
      </li>

      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="/">Log Out</a>
      </li>
    </ul>
  </div>
</nav>


  <% function myfunction(feedback){%>
    <div class="card cardbb text-white mb-3" style="max-width: 18rem;">
      <div class="card-header">Feedback Id: <%=feedback.feedback_id%></div>
        <div class="card-body">
          <p class="card-title">Customer Id: <%=feedback.cust_id%></h5></br>
          <script>
            var val = '<%=feedback.feedback_id%>';
            var loc="/read"+val;
            document.write('<a class="btn btn-primary ngap" href="' + loc + '">Feedback</a>');
            var loc2="/deletefd"+val;
            document.write('<a class="btn btn-primary ngap" href="' + loc2 + '">Delete</a>');
          </script>
        </div>
    </div>
  <% } %>

<div>
  <h1 class="centered colour">All feedbacks</h1>
  <% feedbacks.forEach(myfunction);%>
</div>


<!-- <div class="card text-white bg-secondary mb-3 box4 text-center" style="max-width: 20rem;">
  <div class="card-header">Total Bill</div>
  <div class="card-body">
    <h5 class="card-title">Secondary card title</h5>
    <a href="#" class="btn btn-primary">Pay</a>
  </div>
</div> -->


<%- include('footer') %>
